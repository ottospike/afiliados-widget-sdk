{
  "title": "Arena Panda — Botão (Type1)",
  "description": "Botão 'Arena Panda' portado do app (ArenaButtonType1): billboard das ofertas fixas + reta final (timer <=1h), dados via proxy público do afiliados (/api/widgets/minigames). Estados: __arenaForce('cycle'|'final')."
}
